<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>You speech we write - {% block title %}{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {# {{ asset('css/app.css') }} #}
        {% endblock %}

        {% block javascripts %}
            {# {{ asset('js/app.js') }} #}
        {% endblock %}
    </head>
    <body>
        <main>
                {% block body %}{% endblock %}
        </main>
    </body>
</html>
